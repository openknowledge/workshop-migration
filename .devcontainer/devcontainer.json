{
  "name": "Workshop Migration",
  "forwardPorts": [80, 4001],
  "portsAttributes": {
    "80": {
      "label": "Online Shop",
      "visibility": "public"
    },
    "ghcr.io/devcontainers/features/docker-in-docker:2": {
      "dockerDashComposeVersion": "v2"
    }
  },
  "postCreateCommand": "sudo rm -f /etc/apt/sources.list.d/yarn.list && cd golden-master-tests && npm ci && npx playwright install --with-deps chromium",
  "postStartCommand": ".devcontainer/setup.sh && docker compose up -d && cd golden-master-tests && npm run test:ui:codespaces",
  "customizations": {
    "vscode": {
      "settings": {},
      "extensions": [
        "streetsidesoftware.code-spell-checker",
        "ms-playwright.playwright"
      ]
    }
  },
  "forwardPorts": [80, 4001, 8080, 8832]
}
